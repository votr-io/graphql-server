schema:
  - ./graphql/*.graphql
overwrite: true
generates:
  ./src/GraphQL/generated/resolvers.ts:
    config:
      mappers:
        # Node: ../../types#Node
    plugins:
      - typescript
      - typescript-resolvers
